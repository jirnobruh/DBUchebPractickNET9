<Page x:Class="DbUchebPractikNET9.Pages.ClientPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#FFF2F2F2">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Content="Корзина"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0 0 120 10"
                Width="100"
                Height="35"
                Click="OpenCart_Click"/>

        <!-- Кнопка выхода -->
        <Button Content="Выйти"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0 0 10 10"
                Width="100"
                Height="35"
                Click="Logout_Click"/>

        <!-- Приветствие -->
        <TextBlock x:Name="WelcomeText"
                   Grid.Row="1"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0 20 0 20"/>

        <!-- Каталог техники -->
        <DataGrid x:Name="TechnicGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding TechnicID}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.CategoryTitle}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status.StatusTitle}" Width="150"/>
                <DataGridTextColumn Header="Цена/день" Binding="{Binding PricePerDay}" Width="120"/>
            </DataGrid.Columns>

        </DataGrid>

        <!-- Кнопка добавления в корзину -->
        <Button Content="Добавить в корзину"
                Grid.Row="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Width="180"
                Height="40"
                Margin="0 10 0 0"
                Click="AddToCart_Click"/>
    </Grid>
</Page>