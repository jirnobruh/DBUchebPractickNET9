<Page x:Class="DbUchebPractikNET9.Pages.OrderDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#FFF2F2F2">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <DockPanel Grid.Row="0" Margin="0 0 0 10">
            <Button Content="Назад"
                    Width="100"
                    Height="35"
                    DockPanel.Dock="Left"
                    Click="Back_Click"/>

            <TextBlock Text="Детали заказа"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Информация о заказе -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0 0 0 10">
            <TextBlock x:Name="OrderInfoText" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Таблица позиций заказа -->
        <DataGrid x:Name="ItemsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Technic.Title}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="120"/>
                <DataGridTextColumn Header="Цена/день" Binding="{Binding PricePerDay}" Width="120"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Total}" Width="120"/>
            </DataGrid.Columns>

        </DataGrid>

        <!-- Итоговая сумма -->
        <TextBlock x:Name="TotalText"
                   Grid.Row="3"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Right"
                   Margin="0 10 0 0"/>
    </Grid>
</Page>